
<section class="section_header">
    <div class="container_header">
        <div class="header">
            <div class="logo">
                <h4>Logo</h4>
            </div>
            <% if(user){%>
            <h4 class="header_welcome">Welcome <%= user%></h4>
            <%}%>
            <div class="auth">
                <%if(!userId){%>

                <a class="auth_btn" href="/loginPage">Log In</i></a>
                <a class="auth_btn" href="/signupPage">Sign Up</a>
                <%}%>
                <%if(userId){%>

                    <a  class="auth_btn" href="/logout">Log Out</a>
                <%}%>

            </div>
        </div>
    </div>
</section>